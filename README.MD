First, clone the repository
```
git clone https://github.com/TopMyster/gemCLI/tree/main
```

Put your API key in the .env file
```
GEMINI_API_KEY=your_api_key_here
```

To install, run:

```bash
pip3 install -e .
```

Once installation is complete, use the Gemini CLI with:

```bash
gemini
```
